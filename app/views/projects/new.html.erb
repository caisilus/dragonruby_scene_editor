<%= turbo_frame_tag "modal", data: {controller: "modal"} do %>
  <%= render "form", project: @project %>
  <div id="overlay" class="fixed top-0 left-0 w-full h-full bg-black bg-opacity-75 z-40"></div>
<% end%>
